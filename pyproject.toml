[project]
name = "bambino"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "regex>=2026.4.4",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
